#include "../incl/minishell.h"
/* 
int	parse_redir_in(t_cmd *cmd, t_redir *redir)
{
	cmd->i++;
	if (parse_filename(cmd, redir->file) == FAIL)
		return (FAIL);
	redir->type = REDIR_IN;
	return (SUCCESS);
}
 */